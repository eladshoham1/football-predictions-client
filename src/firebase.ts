// Deprecated: Firebase removed from client. All auth now handled server-side via Google OAuth.
// This file kept for backwards compatibility but exports no-op functions.

export function isFirebaseConfigured(): boolean {
  return false
}

export const auth = null
